name: youtube_downloader_plus
description: "A modified Flutter package based on youtube_downloader_totalxsoftware, developed by TotalXSoftware, with Arabic localization, customizable quality options, and progress notifications."
version: 1.0.3
homepage: "https://github.com/shaaa4t/youtube_downloader_plus"

environment:
  sdk: ^3.5.3
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  video_url_validator: ^1.0.4
  youtube_explode_dart: ^2.5.3
  ffmpeg_kit_flutter_new_min: ^3.1.0
  path_provider: ^2.1.5
  awesome_notifications: ^0.10.1
  permission_handler: ^12.0.1
  flutter_foreground_task: ^9.1.0
  http: ^1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.

flutter:
  generate: true

    #   platforms:
    # android:
    #   package: com.totalxsoftware.razorpay_totalxsoftware
    #   pluginClass: RazorpayTotalxsoftwarePlugin
    # ios:
    #   pluginClass: RazorpayTotalxsoftwarePlugin
